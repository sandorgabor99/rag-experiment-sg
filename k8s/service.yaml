apiVersion: v1
kind: Service
metadata:
  name: rag-pipeline-service
  labels:
    app: rag-pipeline
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: rag-pipeline
